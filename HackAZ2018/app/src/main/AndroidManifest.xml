<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aagua.hackaz2018">

    <uses-permission android:name="android.permission.WRITE_CALENDAR"></uses-permission>
    <uses-permission android:name="android.permission.READ_CALENDAR"></uses-permission>
    <uses-permission android:name="android.permission.SEND_SMS" />>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/choreganizer"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/choreganizer"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        >

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login">

        </activity>
        <activity android:name=".SignUpActivity"></activity>

        <activity android:name=".CalendarActivity">

        </activity>

        <activity android:name=".AdminActivity"></activity>

        <activity android:name=".DayView"></activity>

        <activity android:name=".InputEventNameAndDescription"></activity>

        <activity android:name=".TimePicker"></activity>

        <activity android:name=".sendMessageActivity"></activity>
    </application>

</manifest>